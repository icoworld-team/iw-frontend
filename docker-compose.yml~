version: "2"
services:
    node:
        image: "node"
        user: "node"
        workdir: /app
        environment:
            - NODE_ENV=production
        volumes:
            - ./:/app
        expose:
            - "3000"
        command: "npm start"
