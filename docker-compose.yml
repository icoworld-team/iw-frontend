version: '2'

networks:
  efk_fluentd:
    external: true

services:
  frontend:
    build: .
    image: frontend
    container_name: front
    networks:
      - efk_fluentd
    ports:
      - "80:80"
