version: '2'

networks:
  efk_hostnet:
    external: true

services:
  frontend:
    build: .
    image: frontend
    container_name: front
    networks:
      - efk_hostnet
    ports:
      - "80:80"
