version: '2'

networks:
  efk_fluentd:
    external: true

services:
  frontend:
    build: .
    image: front-${GIT_BRANCH}:0.${BUILD_ID}
    container_name: frontend-${BUILD_ID}
    networks:
      - efk_fluentd
    ports:
      - "80:80"
